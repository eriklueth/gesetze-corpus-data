# data repo must stay clean of build artefacts
.DS_Store
Thumbs.db

# derived/corpus.jsonl is regenerable and too large for the repo.
# Run `gesetze-corpus export` to rebuild it locally.
derived/corpus.jsonl
